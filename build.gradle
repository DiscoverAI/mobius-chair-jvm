plugins {
    id 'scala'
    id 'maven'
    id 'signing'
    id 'com.github.maiflai.scalatest' version '0.26'
    id 'net.researchgate.release' version '2.6.0'
    id 'io.codearte.nexus-staging' version '0.21.2'
}

group = "com.github.meandor"

def scalaCompatVersion = "2.12"
def scalaVersion = "$scalaCompatVersion.10"

dependencies {
    compile "org.scala-lang:scala-library:$scalaVersion"
    compile "com.typesafe.scala-logging:scala-logging_$scalaCompatVersion:3.9.2"
    compile 'org.apache.hadoop:hadoop-client:3.2.1'

    testCompile "org.scalatest:scalatest_$scalaCompatVersion:3.1.2"
    testRuntime 'com.vladsch.flexmark:flexmark-all:0.35.10'
}

repositories {
    mavenCentral()
    mavenLocal()
}

task javadocJar(type: Jar) {
    from javadoc
}

task sourcesJar(type: Jar) {
    from sourceSets.main.allSource
}

artifacts {
    archives javadocJar, sourcesJar
}

signing {
    sign configurations.archives
}

uploadArchives {
    repositories {
        mavenDeployer {
            beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }

            repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
                authentication(userName: ossrhUsername, password: ossrhPassword)
            }

            snapshotRepository(url: "https://oss.sonatype.org/content/repositories/snapshots/") {
                authentication(userName: ossrhUsername, password: ossrhPassword)
            }

            pom.project {
                name 'mobius-chair'
                packaging 'jar'
                description 'Library for managing versions of datasets or models.'
                url 'https://github.com/DiscoverAI/mobius-chair-jvm'

                scm {
                    url 'scm:git@github.com:DiscoverAI/mobius-chair-jvm.git'
                    connection 'scm:git@github.com:DiscoverAI/mobius-chair-jvm.git'
                    developerConnection 'scm:git@github.com:DiscoverAI/mobius-chair-jvm.git'
                }

                licenses {
                    license {
                        name 'Apache-2.0 License'
                        url 'https://raw.githubusercontent.com/DiscoverAI/mobius-chair-jvm/master/LICENSE'
                    }
                }

                developers {
                    developer {
                        id 'dschruhl'
                        name 'Daniel Schruhl'
                        email 'danielschruhl@gmail.com'
                    }
                }
            }
        }
    }
}
